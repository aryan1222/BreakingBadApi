import React from 'react';
import spin from '../img/spinner.gif';

const Spinner = () =>{
    
    return (
        <div className="center">
            <img src={spin} alt=''/>
        </div>
    )
}

export default Spinner;